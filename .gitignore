# Resultados de builds (symlink que genera Nix)
result

# Archivos con información sensible o regenerables
hardware-configuration.nix   # opcional: contiene UUIDs de disco
flake.lock                  # mejor regenerarlo al clonar
hardware-configuration.nix
